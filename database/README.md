# Database

This directory contains database design documentation and migration notes for GameSphere.

The application uses PostgreSQL as its relational database. Database schema changes are managed by Flyway from the Spring Boot backend.
