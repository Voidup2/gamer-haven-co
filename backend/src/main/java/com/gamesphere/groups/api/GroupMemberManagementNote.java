package com.gamesphere.groups.api;

/**
 * Group membership is intentionally kept simple for the current GameSphere scope:
 * public groups can be joined directly, private groups are not joinable through the public API,
 * and only the owner can update or delete a group.
 */
public final class GroupMemberManagementNote {
    private GroupMemberManagementNote() {}
}
